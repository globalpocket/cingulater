{
  "mcpServers": {
    "mcp-routing-gateway": {
      "command": "mcp-routing-gateway",
      "args": [
        "--work-dir", ".",
        "--config", "gateway_config.json",
        "--mcp-config", "gateway_mcp_config.json"
      ]
    },
    "docker": {
      "command": "uvx",
      "args": [
        "mcp-server-docker"
      ]
    }
  }
}
name: sync_wiki
description: "ローカルの .brwn/notes フォルダを外部の Wiki リポジトリと同期（Push/Pull）するワークフロー"
start_node: sync
nodes:
  sync:
    prompt_file: sync_wiki.md
    model: executor
    next: END
triggers:
  - type: cron
    value: "0 0 * * *"

